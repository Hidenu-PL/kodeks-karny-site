Options -MultiViews
RewriteEngine On
RewriteBase /

# Serve existing files and directories as-is.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Let the React application handle deep links such as /kodeks/121.
RewriteRule ^ index.html [L]
